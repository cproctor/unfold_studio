define([


],
function(fetch_story) {
    function Story(id) {
        if (id) {
            this.id = id;
            this.fetch();
        }
    }

    InkFile.prototype = {
        fetch: function() {
            
        }
    }
})
